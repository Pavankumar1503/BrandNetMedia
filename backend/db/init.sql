CREATE DATABASE IF NOT EXISTS brandnetmedia;
USE brandnetmedia;
-- The application uses JPA ddl-auto=update so tables will be created automatically.
-- Insert sample services and prices:
-- (You can add rows via admin endpoints or directly using INSERT statements.)
